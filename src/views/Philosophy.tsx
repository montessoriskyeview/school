import { Box, Typography } from "@mui/material"

export const Philosophy = () => {
  return (
    <Box component={"div"}
      sx={{
        padding: 10
      }}>
      <Typography>
        Philosophy!
      </Typography>
    </Box>
  )
}