import { Box, Typography } from "@mui/material"

export const Schedule = () => {
  return (
    <Box component={"div"}
      sx={{
        padding: 10
      }}>
      <Typography>
        Schedule!
      </Typography>
    </Box>
  )
}