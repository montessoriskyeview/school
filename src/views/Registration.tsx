import { Box, Typography } from "@mui/material"

export const Registration = () => {
  return (
    <Box component={"div"}
      sx={{
        padding: 10
      }}>
      <Typography>
        registration!
      </Typography>
    </Box>
  )
}