import { Box, Typography } from "@mui/material"

export const Location = () => {
  return (
    <Box component={"div"}
      sx={{
        padding: 10
      }}>
      <Typography>
        Location!
      </Typography>
    </Box>
  )
}