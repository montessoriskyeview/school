import { Box, Typography } from "@mui/material"

export const Tuition = () => {
  return (
    <Box component={"div"}
      sx={{
        padding: 10
      }}>
      <Typography>
        Tuition!
      </Typography>
    </Box>
  )
}